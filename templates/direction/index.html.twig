{% extends 'interface.html.twig' %}

{% block title %}Hello DirectionController!{% endblock %}

{% block body %}
   <table id="exemple" class="table table-bordered bs-table-border-color " style="margin-top:-533px;width:1000px;margin-left:auto;">
                    <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th>E-mail</th>
                            <th>Etablissement</th>
                         
                        
                            <th width="100px">Action</th>
                        </tr>
                    </thead>
                    <tbody id="projects-table-body">
                           {% for direction in directions %}
                             <tr>
                                 <td>{{ direction.nom }}</td>
                                 <td>{{ direction.prenom }}</td>
                                 <td>{{ direction.etablissement }}</td>
                                
                            <td>  



                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="7">Aucun enregistrement trouvé</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>

{% endblock %}
{% block javascript %}
  {{parent()}}
        <script>
            $(function() {
                
                $('#example').DataTable({
                    "language": {
                        "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/French.json"
                    }
                  });


                });
    </script>
{% endblock %}

